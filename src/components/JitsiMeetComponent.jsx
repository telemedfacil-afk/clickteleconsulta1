import React from 'react';

// This component is currently inactive/unused as we have moved to opening Jitsi in a new tab.
// It is kept as a placeholder if needed for future reference or reactivation.
const JitsiMeetComponent = ({ appointment, userRole, isOpen, onClose }) => {
  return null;
};

export default JitsiMeetComponent;